defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "post" # previously `post` in Jekyll 2.2.
    values:
      layout: "default"

baseurl: "/AMRLego"
prose:
	siteurl: "http://lavahot.github.io/AMRLego"
    media: "media"
